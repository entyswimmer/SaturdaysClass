# 正解例
---

## 1. models.py

```python
@dataclass
class Transaction:
    date: date
    category: str #要件書を確認
    amount: int
    note: str

    def to_list(self):
        return [self.date.isoformat(), self.category, self.amount, self.note]

```

---

## 2. services.py
```python
#1. import文
from .models import Transaction

#2. FinanceClass
@staticmethod
    def add_entry(is_revenue, trans_data):
        transaction = Transaction(**trans_data)
        type_name = "revenue" if is_revenue else "expenses"
        Storage.save(type_name, transaction)
        return transaction
```
---

## 3. view.py
```python
# 金額
#上からコピーでtext="金額: ", row=3にすればok
ttk.Label(input_frame, text="金額:").grid(row=3, column=0, sticky="e")
        self.ent_amount = ttk.Entry(input_frame)
        self.ent_amount.grid(row=3, column=1, padx=5, pady=2)

def get_input_data(self):
        """入力フィールドの値を辞書で返す"""
        return {
            "is_revenue": self.type_var.get() == "revenue",
            "date": self.ent_date.get(),
            "category": self.ent_category.get(),
            "amount": self.ent_amount.get(),
            "note": self.ent_note.get()
        }
```
---
